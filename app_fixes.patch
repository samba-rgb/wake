diff --git a/src/ui/app.rs b/src/ui/app.rs
index 0000000..0000000 100644
--- a/src/ui/app.rs
+++ b/src/ui/app.rs
@@ -1563,7 +1563,7 @@ impl MonitorApp {
     }
     
     /// Main render function
-    fn render(&mut self, f: &mut Frame) {
+    fn render(&mut self, f: &mut Frame<'_>) {
         // If help is shown, render help screen
         if self.show_help {
             self.render_help(f, f.size());
@@ -1593,7 +1593,7 @@ impl MonitorApp {
     }
 
     /// Render the help screen
-    fn render_help(&self, f: &mut Frame, area: Rect) {
+    fn render_help(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let block = Block::default()
             .title("Wake Pod Monitor Help")
             .borders(Borders::ALL)
@@ -1623,7 +1623,7 @@ impl MonitorApp {
     }
     
     /// Render the tab bar
-    fn render_tabs(&self, f: &mut Frame, area: Rect) {
+    fn render_tabs(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let titles = Tab::all().iter().map(|t| {
             Line::from(vec![
                 Span::styled(
@@ -1651,7 +1651,7 @@ impl MonitorApp {
     }
     
     /// Render overview tab
-    fn render_overview(&self, f: &mut Frame, area: Rect) {
+    fn render_overview(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let block = Block::default()
             .title("Overview")
             .borders(Borders::ALL);
@@ -1687,7 +1687,7 @@ impl MonitorApp {
     }
     
     // Simple render functions for the other tabs - in a full implementation these would show charts
-    fn render_cpu(&self, f: &mut Frame, area: Rect) {
+    fn render_cpu(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let block = Block::default()
             .title("CPU Metrics")
             .borders(Borders::ALL);
@@ -1707,7 +1707,7 @@ impl MonitorApp {
         f.render_widget(paragraph, area);
     }
     
-    fn render_memory(&self, f: &mut Frame, area: Rect) {
+    fn render_memory(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let block = Block::default()
             .title("Memory Metrics")
             .borders(Borders::ALL);
@@ -1727,7 +1727,7 @@ impl MonitorApp {
         f.render_widget(paragraph, area);
     }
     
-    fn render_network(&self, f: &mut Frame, area: Rect) {
+    fn render_network(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let block = Block::default()
             .title("Network Metrics")
             .borders(Borders::ALL);
@@ -1747,7 +1747,7 @@ impl MonitorApp {
         f.render_widget(paragraph, area);
     }
     
-    fn render_disk(&self, f: &mut Frame, area: Rect) {
+    fn render_disk(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let block = Block::default()
             .title("Disk Metrics")
             .borders(Borders::ALL);
@@ -1767,7 +1767,7 @@ impl MonitorApp {
     }
     
     /// Render the status bar at the bottom of the screen
-    fn render_status_bar(&self, f: &mut Frame, area: Rect) {
+    fn render_status_bar(&self, f: &mut Frame<'_>, area: ratatui::layout::Rect) {
         let status_chunks = Layout::default()
             .direction(Direction::Horizontal)
             .constraints([
